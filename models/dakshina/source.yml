version: 2

sources:
  - name: DEV
    schema: DEV
    database: JAPAN
    description: "Raw data ingested from snowflake systems."
    tables:
      - name: customers
        # identifier: customers
        description: "Customer master data from CRM."
        columns:
          - name: customer_id
            tests:
              - not_null
              - unique
        # config:
        #   loaded_at_field: created_at
        #   freshness:
        #     warn_after: {count: 12, period: hour}
        #     error_after: {count: 24, period: hour}
      - name: job_audit
      - name: src_bank_transactions
      - name: SALES